package com.picsou.model;

/** How the owner uses the property. Descriptive only — it does not affect the estimate. */
public enum PropertyCategory {
    PRIMARY_RESIDENCE,
    SECONDARY_RESIDENCE,
    RENTAL,
    LAND,
    OTHER
}
